# Game Console Extraction Plan

This plan aims to extract the Game Console (`window.gameLog`) from `main.js` into its designated module `3-js/8-Console/80-console.js`. This will ensure all DOM-based console reporting logic is fully separated from the main loop and controller.

## User Review Required

> [!IMPORTANT]  
> Please review this extraction plan. As always, I will perform this single folder extraction first, and then stop to wait for your feedback.

## Proposed Changes

### 8-Console Module

#### [MODIFY] [80-console.js](file:///c:/dev/GEM-VP-Circumvade/3-js/8-Console/80-console.js)
- Move the `window.gameLog` function implementation here.
- The script will export a `gameLog` function and also bind it to `window.gameLog` so that all existing systems (like the rules engine) that rely on it can continue using it interchangeably.
- Move the initialization log `gameLog("SYSTEM: Console Initialized", "system");` here to naturally boot the console when imported.

### Main Execution Context

#### [MODIFY] [main.js](file:///c:/dev/GEM-VP-Circumvade/3-js/main.js)
- Remove the `window.gameLog` definition block entirely (lines 6-17).
- Import `gameLog` via a relative path from `./8-Console/80-console.js` at the top of the file to ensure the console system boots before any other systems try to log.

## Open Questions

> [!WARNING]
> Is `8-Console/80-console.js` the extraction you'd like to tackle next, or would you prefer extracting the complex 'Mouse Snapping' logic into the `5-interaction` folder?

## Verification Plan

### Manual Verification
- Check the web app via Live Server.
- When actions occur (like Startup, rules processing, zooming), the on-screen Game Console should continue to display log messages natively in the UI.
